# Copyright 2020 Energinet DataHub A/S
#
# Licensed under the Apache License, Version 2.0 (the "License2");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Publish Energinet.DataHub.MarketParticipant.Client nuget package

on:
  push:
    branches:
      - main
    paths:
      - source/Energinet.DataHub.MarketParticipant.Client/**
      - .github/workflows/publish-marketparticipant-client.yml
  workflow_call: {}
  workflow_dispatch: {}

env:
  PUSH_PACKAGES: ${{ github.event_name != 'pull_request' }}

jobs:
  build_and_publish:
    runs-on: windows-2022
    name: Publish bundle to NuGet.org

    steps:
      - name: Checkout repository
        uses: Energinet-DataHub/.github/.github/actions/nuget-checkout-repository@v10

      - name: Setup dotnet and tools
        uses: Energinet-DataHub/.github/.github/actions/dotnet-setup-and-tools@v10
        with:
          use_azure_functions_tools: "true"
          dotnet_version: 7.0.200

      - name: Build and test solution
        uses: Energinet-DataHub/.github/.github/actions/dotnet-solution-build-and-test@v10
        with:
          solution_file_path: ./source/packages/Energinet.DataHub.MarketParticipant.Libraries.sln
          code_coverage_flags: clientlibraries
          dotnet_version: 7.0.200

      - name: Pack Client project
        uses: Energinet-DataHub/.github/.github/actions/nuget-project-pack@v10
        with:
          project_path: ./source/packages/Energinet.DataHub.MarketParticipant.Client/source/packages/Energinet.DataHub.MarketParticipant.Client/Energinet.DataHub.MarketParticipant.Client.csproj

      - name: Package content or build has changed
        id: changed-content
        uses: tj-actions/changed-files@v17.2
        with:
          files: |
            source/packages/Energinet.DataHub.MarketParticipant.Client/**/*.*
            .github/workflows/publish-marketparticipant-client.yml

      - name: Assert versions of NuGet packages and push them to NuGet.org
        uses: Energinet-DataHub/.github/.github/actions/nuget-packages-assert-and-push@v10
        with:
          push_packages: ${{ env.PUSH_PACKAGES }}
          content_changed: ${{ steps.changed-content.outputs.any_changed }}
          nuget_token: ${{ secrets.nuget_token }}
          nupkg_recurse_path: source
